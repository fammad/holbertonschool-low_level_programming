#include <stdio.h>
#include "main.h"
#include <string.h>
/**
*_strlen - does what it say
*@s: Variable
*Return:Lenght of interger
*/
int _strlen(char *s)
{
  int lenght;

  lenght = strlen(s);
  return (lenght);
}
