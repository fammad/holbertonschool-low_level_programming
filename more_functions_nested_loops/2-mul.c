#include "main.h"
#include <stdio.h>
/**
 *mul - function checks if number is between 0-9
 *@a: variable
 *@b: variable
 *Return: Sum of the file
 *
 */
int mul(int a, int b)
{
  int mul = a * b;
  return (mul);
}
