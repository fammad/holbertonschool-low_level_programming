#include "main.h"
#include <stdio.h>

/**
 *add - adds the tables
 *a - is one number
 *b - is another muber
 *sum - is the result
 */

int add(int, int)
{
	int a, b, sum;

	sum = a + b;

	return (sum);
}
