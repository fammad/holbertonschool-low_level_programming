#include "main.h"
#include <stdio.h>

/**
 *add - adds the tables
 *@a - is one number
 *@b - is another muber
 *@sum - is the result
 */

int add(int a, int b)
{
	int sum;

	sum = a + b;

	return (sum);
}
